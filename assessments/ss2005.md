# Super Shanghai 2005 (Japan, Rev A) (GDL-0031A) (`ss2005`) — portability assessment

## 1. Verdict

| | |
|---|---|
| **Final score** | **54.8 (B)** |
| Bottom line | With the v7 ARAM-content and v8 VRAM FB-mask fields now populated (the prior v6 sidecar predated both and scored those regions through conservative peak-keyed fallbacks), VRAM drops out of contention entirely (0.476× fit, was 1.368× on the address fallback) while ARAM remains the binding region but measurably lighter on real content volume (1.494×, was 1.749× on the fill-excluded address fallback); main is untouched (already content-keyed, 0.406×, byte-identical to v6). The game shipped on a 2004 PS2 CD, so the content demonstrably fits console-class budgets — this stays trim-everything work, and the real measurements now say measurably less trimming than the v6 fallbacks implied. |
| Assessed | capture 2026-08-09 · battery v9 · flycast `f014a410c` · Ghidra 12.1.2_PUBLIC · MAME `59e7c0b` |

## 2. Identity

| | |
|---|---|
| Set / family | `ss2005` (Rev A, GDL-0031A — MAME parent; covers clone `ss2005o`, original GDL-0031). Both revisions share the same security PIC `317-5108-jpn.pic` (CRC `4fa7dede`) and the same default EEPROM `ss2005-default-eeprom.bin`; only the disc SHA1 differs (`gdl-0031` vs `gdl-0031a`) — MAME src/mame/sega/naomi.cpp @59e7c0b lines 8769–8798. No documented Rev A changelog; period listings market it as "New Version" ([eBay](https://www.ebay.com/itm/295583672099)) |
| Maker / year | Starfish, 2005 (arcade April 2005 per [arcadeitalia history](http://adb.arcadeitalia.net/dettaglio_mame.php?game_name=ss2005o)) |
| Genre / format | Puzzle ★ (mahjong-solitaire tile matching), GD-ROM (GDL-0031A, 63.1 MB) |
| Official DC port | No — the arcade release is itself a port of Starfish's 2004 Japan-only PS2 title スーパー上海2005 (arcadeitalia history: "previously appeared on Sony PlayStation 2 (2004)"; [GameFAQs PS2 page](https://gamefaqs.gamespot.com/ps2/924393-super-shanghai-2005), [Glitchwave](https://glitchwave.com/game/super-shanghai-2005/)). No Dreamcast release in the series entry's history; the franchise's only native DC title is the unrelated *Shanghai Dynasty* |
| Community ports | None found (searched 2026-08-03) — absent from the sega-naomi.eu ["Mega list of Naomi to Home Console Ports"](https://www.sega-naomi.eu/forum/viewtopic.php?t=2185); the [Dreamcast Junkyard Naomi GD-ROM article](https://www.thedreamcastjunkyard.co.uk/2016/01/guest-article-expanding-dreamcast.html) lists home ports as "(PS2, DS)" only (the "DS" likely conflates Sunsoft's separate *Shanghai DS*); no dreamcast-talk/DCEmulation conversion threads |
| Representative choice | Rev A (GDL-0031A) — newest revision in the original region (spec §1) and the MAME parent |

## 3. Boot & run evidence

Boots: yes · handoff at 20.0 s (`trigger = "pio"`, GD DIMM ~1 MB bootstrap) · run 600 s ·
rom: `naomi/ss2005.zip` (attempt 1 flaked, attempt 2 clean — see Anomalies)
Attract/demo reached: **demo** — sidecar `capture.coverage = "demo"` (set this pass after
reviewing the shots); the attract loop cycles the title card, three play-mode demos each
stamped DEMO (chain/professional/pop-up Shanghai — `shot-182s.png`, `shot-304s.png`,
`shot-609s.png`) and a HIGH SCORE table (`shot-548s.png`).
Screenshots: `evidence/ss2005/shot-060s.png` · `evidence/ss2005/shot-182s.png` ·
`evidence/ss2005/shot-304s.png` · `evidence/ss2005/shot-548s.png` · `evidence/ss2005/shot-609s.png`
Anomalies: leg 1 attempt 1 hit the known `emulator-exited` flake (`raw/stdout-leg1.log`:
"Verify Failed : &mem_b[0] == ..." dynarec-init abort in `driver.cpp`) and the battery
auto-retried per v2 behavior; attempt 2 (`raw/stdout-leg2.log`) ran the full 600 s window
cleanly (`raw/timeline.json`). All raw memory metrics (main `peak`/
`nz_total`/`nz_above_cap`/`dma_high_water`; VRAM `peak`/`nz_total`/`nz_above_cap`; ARAM
`peak`/`nz_above_cap`) and all streaming metrics reproduced byte-identically against the
v6 figures in this doc's prior §4/§5 — the attract loop is deterministic frame-for-frame
(same pattern seen on tetkiwam/moeru), so the score move below is pure fallback→measured
re-keying, not changed emulator behavior; only the new v7/v8 fields (`vram.content_total`,
`vram.fb_bytes`, `aram.content_total`) are genuinely new measurements. The v2-era capture
was display-blind (stale TA frame, kb §4.m) and twice falsely G1-parked by a wrong
boot-marker grep (kb §4.n, fixed in `f5bf2c4`) — both tooling faults, resolved before v4.

## 4. Memory fit (axis: 30.3)

| Region | Scored value | DC cap | u | Sub-score | Evidence / note |
|---|---|---|---|---|---|
| Main RAM (write-truth content volume, `nz_total`) | 6,815,290 | 16,777,216 | 0.4062 | 100.0 | unchanged byte-for-byte from v6 · address peak 32,505,920 (u 1.938, informational) contains the **shared 64-byte structure at `0x1F00000`–`0x1F0003F`** seen on `kurucham`/`ikaruga` — signature candidate, kb §6 item 3 · nz_above_cap 5,512,235 · `dma_high_water` 27,511,008 (1.64×, byte-identical to v4/v6) |
| VRAM (v8 FB-masked content: `content_total` + 2×`fb_bytes`) | 2,763,089 + 2×614,400 = 3,991,889 | 8,388,608 | 0.4759 | 100.0 | **first real v8 measurement** — v6 had no `content_total`/`fb_bytes` and fell back to the raw address peak (11,476,544, u 1.368, sub-score 35.3, then the second-tightest region); nz_total 2,763,089 (byte-identical to v6) with 2,751,855 of it above the 8 MB address line — confirmed placement/relocation artifact of a CPU-uploaded asset store parked high (the `kurucham` pattern), not volume; `fb_bytes` 614,400 = exactly 640×480×2 |
| ARAM (v7 content volume, `content_total`) | 3,132,410 | 2,097,152 | 1.4937 | 30.3 | **first real v7 measurement, still the binding region** — v6 had no `content_total` and fell back to the fill-excluded content-high address (`peak` 3,666,896, u 1.749, sub-score 20.1); the real content volume is lighter than that address estimate implied, as the fallback-can-only-under-score contract (`content_total <= content_high + 1`) predicted · nz_above_cap 1,435,800 (byte-identical to v6) is real content above cap, not the DMPD fill |

Memory axis = min(100.0, 100.0, 30.3) = **30.3** (was 20.1). ARAM stays the binding region —
its fallback→measured move raises the axis 20.1→30.3 — while VRAM's fallback→measured move
takes it out of contention entirely (35.3→100.0); main is unaffected (already content-keyed
pre-v9 for this title, unchanged since v6).

Watermarks (informational, content-scan — stale-data prone, byte-identical to v6): main
32,505,920 · vram 11,476,544 · aram 8,388,608 (the boot-time "DMPD" fill, not content).

## 5. Cart streaming (axis: 76.1)

DMA events 3,204 · total 80,599,040 B (76.9 MiB) · unique 32,378,880 B (30.9 MiB) ·
re-read ratio 0.5983 · steady-state 7.181 MB/min (`short_window: false`) ·
PIO bootstrap `pio_bytes` 1,049,920 B. (Byte-identical to v6 — deterministic attract
loop, see §3 reproduction note.)

## 6. Guts (axis: 85.0)

Code 1,048,576 B · functions 2,568 · MMIO refs: scif 2, rtc 3, g2ext 52 ·
BIOS vector refs: none · flags: `eeprom_bios`, `serial`, `rtc` → −15. (Byte-identical to
v6 — same ROM, static analysis is capture-independent.)
Carve base `0x8c020000`, entry `0x8c020fe0`, header title `SUPER SHANGHAI 2005`.
SDK strings: Kunoichi2 2.07 / Ninja2 / `syStartCwKn` / AIPKN, KAMUI2, CRI —
all Mar-2001-era Katana-adjacent builds; `guts.sdk_strings` also names the Sega/CRI
sound stack doing the ARAM bank load (`sd2 for DC Ver 2.50.17`, `mwSb`, `MANATEE.DRV`,
CRI ADX/Sofdec) — the same driver family whose banks the official Ikaruga DC port
trimmed 4× to fit 2 MiB (kb §4.d precedent).

## 7. Controls (axis: 100.0)

Cabinet: one 8-way stick and three game buttons (`controls.device_class = stick`). The
game's own INPUT TEST menu (in-binary, `guts.sdk_strings`) lists UP/DOWN/RIGHT/LEFT +
SELECT/CANCEL/HELP + START — 1:1 on a DC pad.
Sources: MAME src/mame/sega/naomi.cpp @59e7c0b INPUT_PORTS `naomi`;
[arcadeitalia ss2005](http://adb.arcadeitalia.net/dettaglio_mame.php?game_name=ss2005)
and [ss2005o](http://adb.arcadeitalia.net/dettaglio_mame.php?game_name=ss2005o)
(8-way joystick + 6-button JVS standard declaration, 2P solo). The
[LaunchBox](https://gamesdb.launchbox-app.com/games/details/39342-super-shanghai-2005)
claim of a "dual-analog control scheme" describes the PS2 pad layout — the binary's
own `ANALOG STICK` / `DIRECTIONAL BUTTON` strings are PS2-origin leftovers — and is
discounted for the cabinet.

## 8. Score computation

final = memory^.40 · streaming^.20 · guts^.20 · controls^.10 · similarity^.10
      = 30.3^.40 · 76.1^.20 · 85.0^.20 · 100.0^.10 · 70.0^.10 = **54.8 (B)**
Similarity inputs: developer no, SDK overlap partial, loader match yes.

## 9. Risks & notes

- **ARAM is now measured, not estimated, and still binds**: the v7 `content_total`
  (3,132,410 B, u 1.494) confirms the port target directly instead of via the
  fill-excluded address estimate — 1,435,800 B of real content sits above the 2 MB cap
  and must be trimmed, with the Ikaruga 4× audio-trim precedent (kb §4.d) as
  released-port proof this class of trim ships. The measured figure is lighter than
  the old 1.75× address fallback implied (now 1.49×), consistent with the fallback's
  can-only-under-score contract.
- **VRAM is now confirmed to genuinely fit**: the v8 FB-masked keying (content
  2,763,089 B + 2×614,400 B framebuffer budget = 3,991,889 B, u 0.476) replaces the old
  1.368× address-peak fallback outright — the address extent was placement (asset store
  parked above the 8 MB line), not volume, exactly as the v6-era note suspected.
- **PS2-CD origin bounds the difficulty**: the 2004 PS2 release means the content fits
  console-class budgets; the Naomi build simply loads whole banks.
- **Rendering must be verified on real DC hardware** (working-style rule): the fork's
  early captures of this title were display-blind (stale TA frame, kb §4.m); v4+
  captures render the demo, and the emulator-side diagnostic is the raw-VRAM decode
  recipe (`FLYCAST_VRAMDUMP` + `vramdump2png.py`, kb §4.m).
- Main watermark 32,505,920 B is byte-identical to `kurucham`'s and equals this title's
  write-truth address peak — part of the shared `0x1F00000` structure/stream-cache
  reading (kb §6 item 3); treat the address extent with suspicion, the content volume
  is the trustworthy figure.
- MAME emulation status for the set is preliminary / imperfect graphics + sound
  (arcadeitalia, mirroring MAME `GAME_FLAGS`) — consistent with a title that trips
  emulator display paths, as observed here.

## 10. History

| Battery | Date | Final | What changed |
|---|---|---|---|
| v2 | 2026-08-03 | PARKED G3-ARAM | Full 8 MiB ARAM bank at boot read as 4.00× cap (DIMM "DMPD" fill, root-cause kb §7); capture display-blind (kb §4.m); two earlier false G1 parks retracted (wrong boot-marker grep, kb §4.n) |
| v4 | 2026-08-04 | 46.5 (B) | Un-parked: the content metric excludes the DIMM fill — real ARAM content 1.75× scores instead of gating; mahjong attract demo renders — kb §7 |
| v6 | 2026-08-07 | 38.5 (C) | Fresh capture; main re-keyed on write-truth address peak 32,505,920 (1.94×) instead of `dma_high_water` — same shared-`0x1F00000`-structure story as `kurucham`; un-park reconfirmed — kb §6 item 3 |
| v9 | 2026-08-08 | 46.5 (B) | Scoring-only re-key (no re-capture): main scored on content volume `nz_total` 6,815,290 (u 0.406); binding region moved to ARAM (20.1) — spec `2026-08-08-main-content-rekey-design.md` |
| v9 | 2026-08-09 | 54.8 B | ranking-groom chunk 4: fresh v9 capture, provenance v6→v9 — first real v7 ARAM-content + v8 VRAM FB-mask measurements replace conservative fallbacks (VRAM 35.3→100.0, no longer binding; ARAM 20.1→30.3, still binding but lighter; memory axis 20.1→30.3); main/streaming/guts reproduced byte-identically vs v6, confirming pure re-keying, not changed behavior |
